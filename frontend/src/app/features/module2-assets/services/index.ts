/**
 * Module 2 Assets - Services Index
 * 
 * Central export point for all asset-related services.
 * This allows for clean imports throughout the application:
 * 
 * Example:
 * import { AssetService } from '@features/module2-assets/services';
 */

export { AssetService, ImportResult, ImportError } from './asset.service';
